machine:
  ruby:
    version: rbx-2.2.6

dependencies:
  pre:
    - gem install jekyll
  post:
    - bundle exec jekyll build:
        timeout: 300 # 5 min

general:
  artifacts:
    - "./_site"

test:
  override:
    - echo "No tests are needed"
