machine:
  ruby:
    version: rbx-2.2.6

dependencies:
  pre:
    - gem install jekyll

general:
  artifacts:
    - "./_site"

dependencies:
  post:
    - bundle exec jekyll build:
        timeout: 300 # 5 min

test:
  override:
    - echo "No tests are needed"
